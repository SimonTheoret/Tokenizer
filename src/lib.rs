use ahash::{AHasher, RandomState};
use std::collections::HashMap;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_get_stats() {
	let vocab HashMap<&str, i32, RandomState> = HashMap::default();
	let mut expected_stats: HashMap<&str, i32, RandomState> = HashMap::default();
	expected_stats.insert()
    }

    fn build_vocab() -> HashMap<&str, i3> {

    }
}
